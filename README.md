# MiniMessenger — прототип

Локальный прототип мессенджера на Express + Socket.IO.

Как запустить:

1. Установите зависимости:

```powershell
npm install
```

2. Запустите сервер:

```powershell
npm start
```

Откройте http://localhost:3000/
