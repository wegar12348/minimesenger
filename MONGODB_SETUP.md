# 🚀 Подключение MongoDB Atlas

## Быстрый старт (3 минуты)

### 1. Регистрация на MongoDB Atlas
1. Открой https://www.mongodb.com/cloud/atlas
2. Нажми **Sign Up** (регистрация бесплатна)
3. Выбери **Create a free cluster** (M0, бесплатный уровень)

### 2. Получи строку подключения
1. В левом меню нажми **Database** → **Clusters**
2. Нажми **Connect** на твоём кластере
3. Выбери **Connection String** (Drivers)
4. Скопируй строку типа:
   ```
   mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```

### 3. Обнови `.env`
Отредактируй файл `.env` в корне проекта:
```env
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/minimessenger?retryWrites=true&w=majority
PORT=3000
```

Замени:
- `YOUR_USERNAME` — твой логин Atlas
- `YOUR_PASSWORD` — твой пароль Atlas
- `YOUR_CLUSTER` — название кластера (например `cluster0`)

### 4. Запусти сервер
```bash
npm start
```

Если все верно, увидишь:
```
✓ MongoDB connected
🚀 Server started on http://localhost:3000
📱 Access from phone: http://<your-ip>:3000
```

---

## Альтернативы (без регистрации)

### Вернуться на JSON (быстро)
Если не хочешь регистрироваться на Atlas — я верну JSON-хранилище за 2 минуты.
Скажи "верни JSON" и я откатиться обратно.

---

## Помощь
- Ошибка подключения? Проверь правильность пароля в `.env`
- Firewall блокирует? Добавь свой IP в **Network Access** на Atlas
